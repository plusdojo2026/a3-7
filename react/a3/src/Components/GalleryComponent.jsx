const Gallery =() => {
    return (
        <section>
            <h4>＜ベストショット＞</h4>
            <button>追加</button>

            <div>
                画像表示場所
            </div>
        </section>
    );
};

export default Gallery;